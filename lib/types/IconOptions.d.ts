import { Color, Size } from '.';
import { Icon } from 'leaflet';
export declare class IconOptions {
    iconSize: Size;
    icon: Icon;
    iconColor: Color;
    constructor(iconSize: Size, icon: Icon, iconColor: Color);
}
