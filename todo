* set default marker when creating markers
* find solution to compare colors ( https://github.com/dsteinman/ts-color-class )
* central point for storing style?
* change logic so all styleable things are set in layer.options

FormElements
  * Allow configuration of the titles (by strings inthe config)
  * Let the FormElements decide when they should be shown
  * Remove title from constructor and read from options
Styling
  * How to use Leaflet icon?
PopUpElement
  * retain text and allow more then 1 letter
  * TextArea does not fit into parent
DashElement
  * Set default value?
SizeElement
  * Style the SizeElement
IconElement
  * Highlight selected icon
  * Fix color seelction when selecting new icon
Popup
  * Fix text changes not reflected
MakiMarker
  * Replace calls to mapbox api

Map:
  * Remove marker focus box when leaving focus
